import LoginForm from './LoginForm'

// Generic login: starts with "type your company short name" step.
// The branded counterpart is `app/login/[shortName]/page.tsx`.
export default function LoginPage() {
  return <LoginForm />
}
